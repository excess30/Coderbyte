#include <iostream>
#include <string>
using namespace std;

int FirstFactorial(int num) { 

   if (num == 0) 
    return 1; 
    return num * FirstFactorial(num - 1); 
  return num; 
            
}

int main() { 
  
  cout << FirstFactorial(gets(stdin));
  return 0;
    
}
